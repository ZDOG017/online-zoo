# API unit tests

| File | Tests |
|------|--------|
| `client.test.ts` | `request()` — HTTP contract |
| `endpoints.test.ts` | `getPets`, `getPetById` — routes & JSON shapes |

See **`TESTING.md`** in the repository root for the full map and mentor-oriented overview.
